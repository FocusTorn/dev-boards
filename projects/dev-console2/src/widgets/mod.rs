/// Reusable TUI components and visual building blocks.
///>
/// The `widgets` module provides the library of UI elements used to construct 
/// the `dev-console-v2` interface. Each component is designed to be 
/// independent, adhering to the project's mandates for performance, 
/// delegated event handling, and reactive state projection.
///<
pub mod tab_bar;
pub mod progress_bar;
pub mod status_box;
pub mod command_list;
pub mod smooth_scrollbar;
pub mod toast;
